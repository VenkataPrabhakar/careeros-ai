# Frontend

Next.js 15 frontend for CareerOS AI.

## Scripts

```bash
npm install
npm run dev
npm run lint
npm run build
```

## API base URL

By default the UI calls `http://localhost:8080/api`.

To point at a different backend, set:

```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api
```
